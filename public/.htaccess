<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

  RewriteCond %{HTTP_HOST} ^ still-inlet-71132.herokuapp.com[NC]
    RewriteCond %{SERVER_PORT} 80
    RewriteRule ^(.*)$ https://still-inlet-71132.herokuapp.com/$1 [R,L]
   
</IfModule>
